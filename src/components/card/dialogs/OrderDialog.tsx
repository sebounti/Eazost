"use client";

import OrderCardForm from "@/components/card/forms/OrderCardForm";

const OrderDialog = () => {
  return (
    <OrderCardForm onSubmit={() => {}} />
  );
};

export default OrderDialog;

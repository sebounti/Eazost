import { useState } from "react";

type OrderCardFormProps = {
	onSubmit: (data: any) => void;
};


const OrderCardForm = ({ onSubmit }: OrderCardFormProps) => {
  return (
    <div>
      <h1>OrderCardForm</h1>
    </div>
  );
};

export default OrderCardForm;
